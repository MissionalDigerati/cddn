$('table#projects tbody').append('<%= escape_javascript render(@projects) %>');
<% if (@projects.current_page < @projects.num_pages) %> console.log("im am here");
  $('.pagination').replaceWith('<%= escape_javascript paginate(@projects, :theme => "twitter-bootstrap") %>');
<% else %>
  $('.pagination').remove();
<% end %>
<% sleep 1 %>