$('table#events tbody').append('<%= escape_javascript render(@events) %>');
<% if (@events.current_page < @events.num_pages) %> console.log("im am here");
  $('.pagination').replaceWith('<%= escape_javascript paginate(@events, :theme => "twitter-bootstrap") %>');
<% else %>
  $('.pagination').remove();
<% end %>
<% sleep 1 %>