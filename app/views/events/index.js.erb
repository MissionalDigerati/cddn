$('table#events tbody').append('<%= escape_javascript render(@events, dash: false) %>');
<% if (@events.current_page < @events.num_pages) %> 
  $('.pagination').replaceWith('<%= escape_javascript paginate(@events, :theme => "twitter-bootstrap") %>');
<% else %>
  $('.pagination').remove();
<% end %>
<% sleep 1 %>